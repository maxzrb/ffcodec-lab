# 版本迭代记录

用途：面向中文用户记录版本发布、重要变更、影响范围、验证结果和迁移说明。仅当项目版本或发布状态变化时更新。

## 当前版本

- 版本号：0.5.0
- 发布日期：2026-07-11
- 状态：v0.5.0 成品版 — 正式页面全控件修复、功能补齐与 UI 成品化

### v0.5.0 成品发布

- **正式页面交互闭环**：统一修复特殊参数、preset/profile/tune/pixelFormat、编码器选择等控件的配置绑定与切换规范化。
- **跨平台硬件编码**：新增 AMD AMF 与 Apple VideoToolbox，视频编码器达到 11 个。
- **高级画面处理**：裁剪、旋转、镜像、亮度/对比度/饱和度/伽马、去隔行、锐化均进入统一滤镜链。
- **字幕和流控制**：支持轨道增删、外挂流索引、听障标记、常用 ASS 样式与显式流映射。
- **完整产品能力**：新增六阶段自定义参数、浏览器持久化、URL Hash 分享、响应式深色工作台 UI。
- **工程质量**：ESLint 纳入统一检查，297 项测试、目录审计 0/0、10/10 验收配置与生产构建全部通过。
- **成品发布**：Sites 私有站点已上线：https://ffcodec-lab.maxzhurb.chatgpt.site
- **环境限制**：当前应用内浏览器无可用实例，未完成真实浏览器截图巡检；不影响自动化交互、命令和构建验收。

### v0.4.1 热修复
- **修复正式 BuilderPage checkbox 交互**：specialParameters 全部 31 个参数添加 configBinding，统一读写路径
- **根本原因**：configBinding 缺失 + getByPath 无法读取 flat Record 含点键 + applyFieldChange 回退正则不匹配
- **命令构建器修正**：使用 encoder.specialParameters + commandBinding（不再遍历 Object.entries 将 key 当 FFmpeg 参数）
- **测试加强**：新增 6 个 React Testing Library 集成测试与 4 项命令/契约测试，255/255 全部通过
- **绑定完整性**：补齐 libopus application/frameDuration 与 FLAC sampleFormat，31/31 特殊参数全部显式绑定
- **命令正确性**：布尔特殊参数稳定输出 1/0 或 on/off，消除 NVENC 重复 `-spatial_aq`
- **Playwright 移除**：不再作为项目依赖或默认检查
- 零 `as any` / `@ts-ignore`，原有 245 测试 0 弱化

---

## 历史版本

### v0.4.0
- 发布日期：2026-07-10
- 状态：第四阶段完成 — 架构收口、交互完整性、PresetManager UI、Intel QSV 编码器接入
- 已知缺陷：正式 BuilderPage 中 specialParameters 复选框无法交互（已在 v0.4.1 修复）

### 架构收口
- **configBinding 全量迁移**：22 个 controls 完成 configBinding，旧 `getControlValue` 模式匹配回退删除
- **统一字段变更入口**：`apply-field-change.ts` — React 不再解析 ConfigPath
- **路径安全**：废弃任意字符串 ConfigPath，统一使用 `CONFIG_PATHS` 常量
- **目录审计 0/0**：0 errors, 0 warnings

### 交互完整性
- **全量控件审计**：所有业务控件链路验证（操作→Config→Store→normalization→rules→AST→命令）
- **交互调试面板**：开发模式追踪完整交互链路
- **56 个契约测试**：binding 完整性/类型兼容/值往返/选项切换
- **复选框专项**：全部使用 `checked`/`e.target.checked`/`??` — 零缺陷

### PresetManager 正式 UI
- 预设列表（内置+用户）、创建/编辑/另存为/覆盖/删除/重命名
- JSON 导入导出（Zod 验证 + migration + 非法整体拒绝）
- 5 个内置预设一键应用
- Catalog-driven 预设摘要（零硬编码编码器/容器 ID）

### Intel QSV 编码器
- **h264_qsv, hevc_qsv**：各 6 种质量模式（CQP/ICQ/LA_ICQ/VBR/CBR/LA_VBR）
- **与 NVENC 完全独立**：独立参数体系、无复用、无别名
- **硬件代际分级**：Sandy Bridge→Broadwell→Skylake 分代标注
- **QSV 来源核验**：Intel Media Driver + FFmpeg qsvenc.c 交叉核验
- **环境可用性提示**：hardware-dependent 仅提示，不阻止复制

### 测试和文档
- **245 单元测试**（+108 vs v0.3.0）
- **18 个 E2E 场景**（代码就绪，Playwright 依赖环境）
- **7 份新增/更新文档**：用户指南、编码器可用性、QSV 来源映射、验收记录、审计清单、Warning 登记、CHANGELOG

### 影响范围
- 新增 17 文件，修改 13 文件
- 现有架构零破坏，137 原有测试全部保留
- 视频编码器：7 个（libx264, libx265, libsvtav1, h264_nvenc, hevc_nvenc, h264_qsv, hevc_qsv）

### 验证情况
- TypeScript strict：0 errors
- 目录审计：0 errors, 0 warnings
- 单元测试：245/245 passed (13 files)
- 生产构建：403 KB JS + 1.3 KB CSS
- Git：8 个 Phase 4 独立提交

### 迁移说明
- `RateControlModeId` 新增 6 个 QSV 专用值
- `EncoderImplementation` 新增 `'intel'`
- configBinding 成为所有 quality mode controls 的必填字段（审计为 error）
- 主要变化：
  - **参数核验模型修正**：`SourceAuthority` + `VerificationLevel` 分离，取代旧的 `status: verified` 语义
  - **字段解析层**：`src/domain/presentation/` — 纯 TypeScript，将 Config+Catalog+Rules 转为 UI 可渲染的 `ResolvedField`
  - **正式 BuilderPage**：6 个可折叠参数区域，零 FFmpeg 硬编码，所有控件由 `ResolvedField` 驱动
  - **参数解释面板**：显示说明、影响评估、来源引用和核验状态，从字段？按钮和命令 token 均可打开
  - **命令预览**：Bash/PowerShell/CMD 切换、单行/多行、复制、token 点击定位控件、两遍编码双命令显示
  - **字幕混流/烧录 UI**：内外挂字幕、容器自适应编码、基础 style 设置、与 copy 模式冲突检测
  - **预设与持久化**：localStorage 存储、CRUD 操作、JSON 导入导出、Zod 验证、schema migration、5 个内置预设
  - **测试扩充**：76 单元测试（+43 new：25 presentation + 18 presets）
  - **验收**：10 个手工命令案例全部通过，记录在 `docs/mvp-acceptance.md`
- 影响范围：新增 16 文件，修改 15 文件；原有架构和测试零破坏
- 验证情况：TypeScript 0 错误、目录审计 0 错误 0 警告、76 测试通过、生产构建成功（263KB）、10/10 验收通过
- 已知风险：5 项参数 `needsCrossVerification: true`、PresetManager UI 组件待开发、无 React 组件测试
- 迁移说明：类型系统新增 `sourceAuthority`/`verificationLevel`/`needsCrossVerification` 必填字段，所有编码器和参数数据已更新

## 历史版本

### 0.1.0 (2026-07-10)

- 首轮开发完成（内部里程碑，非正式发布）
- 项目骨架、核心类型系统、5 编码器 + 4 容器数据、11 条规则、Command AST + 3 种 Shell 渲染器、33 项单元测试
- 5 项参数待核验、字幕 UI 和参数解释面板待开发
