# FFCodec Lab 版本规则

FFCodec Lab 的每次正式发布同时使用“语义版本号”和“版本序号”。

## 展示格式

产品界面和面向用户的文档统一显示：

```text
主版本号.次版本号.修订号（版本序号）
```

例如：`1.4.1（9）`。

- Git 标签与 GitHub Release 标签仍使用 `v1.4.1`，以兼容 SemVer 和现有下载链接。
- npm/Electron `package.json` 的 `version` 仍使用合法 SemVer `1.4.1`。
- 中文全角括号中的版本序号只用于产品与文档展示，不写入 SemVer 字段。

## 语义版本号

- 修订号（PATCH）：小更新、小修复、兼容性调整，例如 `1.4.0` → `1.4.1`。
- 次版本号（MINOR）：大更新、大修复、显著但向后兼容的能力变化，例如 `1.4.1` → `1.5.0`。
- 主版本号（MAJOR）：重构类型、架构级或存在破坏性兼容影响的变化，例如 `1.5.0` → `2.0.0`。

升级次版本号时修订号归零；升级主版本号时次版本号和修订号均归零。

## 版本序号

- 从 `1` 开始，每次正式发布递增 `1`，永不重置。
- 正式产品版本标签计入序号；即使对应 GitHub Release 页面后来撤下，只要该版本曾正式发布并保留产品版本标签，仍占用序号。
- 迁移、备份或内部标记标签不计入序号，例如 `pre-monorepo-web`。
- 预发布版本默认不占用正式序号；只有转为正式发布时才递增。
- 已使用的版本序号不得复用或回退。

## 当前基线

截至 2026-07-28，正式版本标签依次为：

| 版本序号 | 版本号 |
| ---: | --- |
| 1 | 0.3.0 |
| 2 | 1.2.0 |
| 3 | 1.2.1 |
| 4 | 1.2.2 |
| 5 | 1.2.3 |
| 6 | 1.2.4 |
| 7 | 1.3.0 |
| 8 | 1.4.0 |
| 9 | 1.4.1 |
| 10 | 1.5.0 |
| 11 | 1.5.1 |
| 12 | 1.6.0 |
| 13 | 1.7.0 |
| 14 | 1.7.1 |

下一次正式发布必须从版本序号 `15` 开始。

## 发布同步清单

每次发布至少同步以下位置：

1. 根目录、Web、Desktop 的 `package.json` SemVer。
2. Workbench 产品界面的“版本号（版本序号）”。
3. 与界面版本相关的自动化测试。
4. README 当前版本、下载链接和发布门禁说明。
5. `version/版本迭代记录.md`、`docs/codex/STATUS.md` 和 `version/工作进度.md`。
6. 注解 Git 标签、GitHub Release 标题/说明、Windows 发布资产和 SHA256SUMS。

## Web 发布渠道

- Web 正式生产环境为 Cloudflare，由 GitHub `master` 推送自动触发构建和部署。
- 不使用 OpenAI Sites 发布 Web 正式版本，也不把 Sites 版本或部署状态作为发布完成条件。
- 发布后以 `https://fflab.loliland.cn` 的实际页面、版本显示和静态资源响应头作为 Web 线上验收依据。
