# v0.4.1 人工交互验收清单

## 验收环境

- 启动开发服务器：`npm run dev` → `http://localhost:5173`
- 浏览器：由人工选择
- 测试方法：逐项操作，记录结果

## 视频编码器验收

### libx264 (默认)

| 项目 | 结果 |
|------|------|
| 编码器选择生效 | pass/fail |
| CRF 值可修改 | pass/fail |
| Preset 可选 | pass/fail |
| Profile 可选 | pass/fail |
| Tune 可选 | pass/fail |
| 命令包含 `-c:v libx264` | pass/fail |

### libx265

| 项目 | 结果 |
|------|------|
| 切换到 libx265 | pass/fail |
| CRF 值可修改 | pass/fail |
| 命令包含 `-c:v libx265` | pass/fail |

### libsvtav1

| 项目 | 结果 |
|------|------|
| 切换到 libsvtav1 | pass/fail |
| CRF 值可修改 | pass/fail |
| 命令包含 `-c:v libsvtav1` | pass/fail |

### h264_nvenc

| 项目 | 结果 |
|------|------|
| 切换到 h264_nvenc | pass/fail |
| **空间自适应量化 checkbox**: 可以点击 | pass/fail |
| **空间自适应量化 checkbox**: checked 状态改变 | pass/fail |
| **空间自适应量化 checkbox**: 命令中 `-spatial_aq` 改变 | pass/fail |
| **空间自适应量化 checkbox**: 可逆切换 (true→false→true) | pass/fail |
| **时间自适应量化 checkbox**: 可以点击 | pass/fail |
| **时间自适应量化 checkbox**: checked 状态改变 | pass/fail |
| **时间自适应量化 checkbox**: 命令中 `-temporal_aq` 改变 | pass/fail |
| **时间自适应量化 checkbox**: 可逆切换 (false→true→false) | pass/fail |

### hevc_nvenc

| 项目 | 结果 |
|------|------|
| 切换到 hevc_nvenc | pass/fail |
| **空间自适应量化 checkbox**: 可以点击 | pass/fail |
| **空间自适应量化 checkbox**: checked 状态改变 | pass/fail |
| **空间自适应量化 checkbox**: 可逆切换 | pass/fail |
| **时间自适应量化 checkbox**: 可以点击 | pass/fail |
| **时间自适应量化 checkbox**: checked 状态改变 | pass/fail |
| **时间自适应量化 checkbox**: 可逆切换 | pass/fail |

### h264_qsv

| 项目 | 结果 |
|------|------|
| 切换到 h264_qsv | pass/fail |
| **低功耗模式 checkbox**: 可以点击 | pass/fail |
| **低功耗模式 checkbox**: checked 状态改变 | pass/fail |
| **低功耗模式 checkbox**: 命令中 `-low_power` 改变 | pass/fail |
| **低功耗模式 checkbox**: 可逆切换 | pass/fail |

### hevc_qsv

| 项目 | 结果 |
|------|------|
| 切换到 hevc_qsv | pass/fail |
| **低功耗模式 checkbox**: 可以点击 | pass/fail |
| **低功耗模式 checkbox**: checked 状态改变 | pass/fail |
| **低功耗模式 checkbox**: 可逆切换 | pass/fail |

## 画面参数验收

| 项目 | 结果 |
|------|------|
| 分辨率模式切换 (source/width/height/size) | pass/fail |
| 帧率模式切换 (source/value) | pass/fail |

## 音频参数验收

| 项目 | 结果 |
|------|------|
| 音频处理模式 (encode/copy/disabled) | pass/fail |
| 切换到 libopus | pass/fail |
| **VBR 开关 checkbox**: 可以点击 | pass/fail |
| **VBR 开关 checkbox**: checked 状态改变 | pass/fail |
| **VBR 开关 checkbox**: 可逆切换 | pass/fail |

## 字幕参数验收

| 项目 | 结果 |
|------|------|
| 字幕烧录启用 checkbox | pass/fail |
| 字幕烧录 checkbox 可逆切换 | pass/fail |

## 封装设置验收

| 项目 | 结果 |
|------|------|
| 容器选择器可切换 | pass/fail |
| 覆盖已有文件 checkbox: 可以点击 | pass/fail |
| 覆盖已有文件 checkbox: checked 状态改变 | pass/fail |
| 覆盖已有文件 checkbox: 可逆切换 | pass/fail |
| 命令中 `-y` 出现/消失 | pass/fail |

## 命令预览验收

| 项目 | 结果 |
|------|------|
| Bash 命令正确生成 | pass/fail |
| PowerShell 切换生效 | pass/fail |
| CMD 切换生效 | pass/fail |
| Token 点击可定位字段 | pass/fail |
| 错误状态阻止复制 | pass/fail |

## 预设验收

| 项目 | 结果 |
|------|------|
| 预设管理面板可打开 | pass/fail |
| 内置预设显示正确 | pass/fail |
| 应用预设后命令更新 | pass/fail |

---

## 验收结论

- 验收人：________________
- 验收日期：2026-07-____
- 全部 checkbox 通过：是 / 否
- 可以发布 v0.4.1：是 / 否
- 备注：
